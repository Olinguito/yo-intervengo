article
    h3 {{r.name}}
    button.follow(ng-class="r.type",ng-click="user.follow(r)") {{ user.follows(r) ? 'abandonar':'seguir' }}
    p.info \#{{r.id}}
    p {{r.description}}
    ul.photos
        li(ng-repeat="p in r.photos"): img(ng-src="")

article
    h3 Ubicación
    p {{r.location.address}},{{r.location.city}},{{r.location.state}}
    leaflet.map(center="center", defaults="mapDefaults", markers="marker")

article.work(ng-if="r.type=='complaint'")
    h3 Obra asociada
    p(ng-hide="r.pubwork") Esta queja actualmente no tiene ninguna obra asociada.
    div(ng-show="r.pubwork")
        p ID Contrato
            i.val {{w.CIIU}}
        p VALOR
            strong.val.important {{w.value_contract | currency}}
        p Contratista
            strong.val {{w.employer}}
        p Contratante
            strong.val {{w.contractor}}
        p Fecha de Inicio
            i.val {{w.date_start | date}}
        p Fecha de Finalización
            i.val {{w.date_end | date}}

