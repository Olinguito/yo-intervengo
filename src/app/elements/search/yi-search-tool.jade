template
  require(from="../icon/yi-icon")

  form
    label(for="search-field")
      yi-icon(icon="search")
    input(
      id="search-field", 
      value.bind="query"
      type="search", 
      placeholder="Busca entre las quejas y peticiones ..."
    )
