template
  require(from="../selector/yi-button")

  yi-button(icon="picture", title="${title}", click.trigger="inputFile.click()")
  input(type="file", accept="image/jpeg, image/png", change.trigger="fileChosen()")
