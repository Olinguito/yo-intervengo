template
  import(from="../selector/yi-button")

  yi-button(icon="photo", title="${title}", click.delegate="inputFile.click()")
  input(type="file", accept="image/*", change.delegate="fileChosen()")
